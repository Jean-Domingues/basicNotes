const retornaQuadrado = numeros => (
  numeros.forEach(numero => {
    const novoNumero = numero ** 2;
    console.log(`O dobro de ${numero} é ${novoNumero}`)
  })
)

const meusNum = [1,2,3,4,5,6,7,8,9]

// retornaQuadrado(meusNum);

console.log(['a', 'b', 'c'].entries(1));
